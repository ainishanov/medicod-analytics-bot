# 🧠 AI-анализ с GLM-4.6

Подробное руководство по функциям искусственного интеллекта в Medicod Analytics Bot.

## 🎯 Обзор

Бот использует **GLM-4.6** от ZhipuAI для автоматического анализа бизнес-метрик и генерации actionable инсайтов.

## 🚀 Основные возможности

### 1. Сравнительный анализ
AI сохраняет последние **8 недель** данных и сравнивает текущие показатели с историческими:

- Рост/падение выручки
- Изменение среднего чека
- Динамика использования функций
- Тренды по ошибкам

### 2. Автоматическая детекция аномалий

Встроенные алгоритмы выявляют:

**Высокая серьезность (⚠️):**
- Падение платежей более чем на 50% за 2 дня
- Критические технические проблемы

**Средняя серьезность:**
- Более 50 ошибок за неделю
- Webhook проблемы

**Низкая серьезность:**
- Неиспользуемые функции (OCR = 0 при активной базе)
- Аномальные паттерны использования

### 3. Умные рекомендации

AI генерирует конкретные, actionable рекомендации:

- Приоритизация задач (ВЫСОКИЙ/СРЕДНИЙ/НИЗКИЙ)
- Конкретные эксперименты для A/B тестов
- Технические улучшения
- Маркетинговые идеи

### 4. Стратегические вопросы

AI задает вопросы, которые помогают думать о продукте:

- Почему функция не используется?
- Как улучшить монетизацию?
- В чем причина падения метрик?
- Какие эксперименты стоит провести?

## 📊 Структура AI отчета

```
🤖 AI ИНСАЙТЫ

🔍 КЛЮЧЕВЫЕ ИНСАЙТЫ (2-3 пункта)
- Важные наблюдения
- Тренды и паттерны
- Сравнение с прошлой неделей

⚠️ ПРОБЛЕМЫ И РИСКИ (1-2 пункта)
- Критические проблемы
- Потенциальные угрозы

💡 РЕКОМЕНДАЦИИ (2-3 действия)
- Конкретные шаги для улучшения
- Приоритизированные задачи

❓ ВОПРОСЫ ДЛЯ РАЗМЫШЛЕНИЯ (2-3 вопроса)
- Стратегические вопросы
- Идеи для экспериментов
```

## 🛠 Техническая реализация

### Архитектура

```
analyticsService.js
  ↓ собирает метрики
  ↓ вызывает
aiAnalysisService.js
  ↓ создает промпт
  ↓ отправляет в GLM-4.6
  ↓ получает анализ
  ↓ возвращает форматированный результат
telegramService.js
  ↓ отправляет в Telegram
```

### Ключевые компоненты

**aiAnalysisService.js:**
- `analyzeReport(report)` - основной метод анализа
- `createAnalysisPrompt(report)` - генерация промпта для AI
- `detectAnomalies(report)` - локальная детекция аномалий
- `addHistoricalData(report)` - хранение истории

**Параметры модели:**
```javascript
{
  model: 'glm-4-flash',
  temperature: 0.7,
  max_tokens: 1000
}
```

### Хранение данных

История сохраняется в памяти сервиса:
- Последние 8 недель
- Автоматическая ротация старых данных
- Структура: `{ date, payments, errors, features }`

## 🔧 Настройка

### Переменные окружения

```env
# Обязательно для работы AI
ZHIPUAI_API_KEY=your_api_key_here

# Включить/выключить AI (по умолчанию: true)
AI_ANALYSIS_ENABLED=true
```

### Отключение AI

Если `ZHIPUAI_API_KEY` не задан или `AI_ANALYSIS_ENABLED=false`:
- Бот продолжает работать
- Отчеты отправляются без секции "🤖 AI ИНСАЙТЫ"
- Базовая детекция аномалий все равно работает

## 💰 Стоимость

**GLM-4-Flash** (используемая модель):
- Очень доступная модель
- ~1000 символов на запрос
- Один отчет = ~0.001-0.003 USD
- В месяц (4 отчета) = ~0.01 USD

## 🎨 Кастомизация промпта

Промпт можно настроить в `aiAnalysisService.js:50`:

```javascript
createAnalysisPrompt(currentReport) {
  let prompt = `Ты эксперт по бизнес-аналитике...`;

  // Добавить свои инструкции:
  prompt += `
  ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ:
  - Фокус на ROI
  - Сравнение с индустриальными бенчмарками
  - Рекомендации по маркетингу
  `;

  return prompt;
}
```

## 🧪 Тестирование

### Локальное тестирование

```bash
# Установить зависимости
npm install

# Создать .env с API ключом
echo "ZHIPUAI_API_KEY=your_key" > .env
echo "AI_ANALYSIS_ENABLED=true" >> .env

# Отправить тестовый отчет
npm run send-report
```

### Проверка работы AI

Посмотреть логи:
```bash
journalctl -u medicod-analytics-bot -f | grep "AI"
```

Ожидаемые логи:
```
🤖 Запрос AI анализа...
✅ AI анализ получен
```

## 📈 Примеры инсайтов

### Пример 1: Падение трафика
```
🔍 КЛЮЧЕВЫЕ ИНСАЙТЫ
• Резкое падение платежей на 79% после 1 ноября
• Возможные причины: выходные, технические проблемы

💡 РЕКОМЕНДАЦИИ
1. Проверить логи на наличие ошибок
2. Сравнить с аналогичным периодом прошлого месяца
```

### Пример 2: Неиспользуемые функции
```
❓ ВОПРОСЫ ДЛЯ РАЗМЫШЛЕНИЯ
• OCR используется 0 раз - проблема в UX или маркетинге?
• Стоит ли сделать первый OCR бесплатным?

💡 РЕКОМЕНДАЦИИ
1. A/B тест: бесплатный OCR для новых пользователей
2. Добавить промо-баннер OCR функции в приложении
```

### Пример 3: Рост среднего чека
```
🔍 КЛЮЧЕВЫЕ ИНСАЙТЫ
• Средний чек вырос на 11% - отличный тренд!
• Пользователи стали чаще покупать премиум анализы

💡 РЕКОМЕНДАЦИИ
1. Масштабировать успешную стратегию
2. Рассмотреть пакетные предложения (3 анализа со скидкой)
```

## 🔒 Безопасность

- API ключ хранится в `.env` (не коммитится в Git)
- Все данные передаются через HTTPS
- История данных хранится только в памяти процесса
- Нет персональных данных пользователей в анализе

## 🆘 Troubleshooting

### AI анализ не работает

1. Проверить наличие API ключа:
   ```bash
   cat .env | grep ZHIPUAI_API_KEY
   ```

2. Проверить логи на ошибки:
   ```bash
   journalctl -u medicod-analytics-bot -n 50 | grep "AI\|Error"
   ```

3. Тестовый запрос к API:
   ```bash
   curl -X POST https://open.bigmodel.cn/api/paas/v4/chat/completions \
     -H "Authorization: Bearer $ZHIPUAI_API_KEY" \
     -H "Content-Type: application/json" \
     -d '{"model":"glm-4-flash","messages":[{"role":"user","content":"test"}]}'
   ```

### Слишком общие инсайты

Попробуй:
1. Накопить больше исторических данных (2-3 недели)
2. Настроить промпт с более конкретными инструкциями
3. Добавить контекст о вашем бизнесе в промпт

## 🚀 Что дальше?

Идеи для улучшения AI-анализа:

1. **Persistence** - сохранять историю в файл/БД
2. **Бенчмарки** - сравнение с индустриальными метриками
3. **Прогнозы** - ML модели для предсказания выручки
4. **Персонализация** - учет специфики вашего бизнеса
5. **Интерактивность** - возможность задавать вопросы AI через Telegram

## 📚 Ресурсы

- [ZhipuAI Documentation](https://open.bigmodel.cn/dev/api)
- [GLM-4 Model Card](https://open.bigmodel.cn/dev/howuse/model)
- [SDK GitHub](https://github.com/MetaGLM/zhipuai-sdk-nodejs-v4)

---

Создано с использованием Claude Code и GLM-4.6 🤖
